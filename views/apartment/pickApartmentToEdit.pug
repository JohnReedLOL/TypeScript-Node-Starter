extends ../layout

block content
  .page-header
    h3.text-center #{title}
    hr

  ul
    each apartment in apartments
        li
            a(href="/account/update-listing/" + apartment.apartmentNumber) Apartment #{apartment.apartmentNumber}