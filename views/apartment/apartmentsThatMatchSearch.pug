extends ../layout

block content
  .page-header
    h3 #{title}
    hr

  ul
    each apartmentNumber in apartmentNumbers
        li
            a(href="/apartment/" + apartmentNumber) Apartment #{apartmentNumber}